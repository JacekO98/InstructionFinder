
@if (string.IsNullOrWhiteSpace(PdfPath))
{
    
}
else
{
    <iframe src="@PdfUrl"
            @key="PdfUrl"
            width="100%"
            height="800px"
            style="border:none">
    </iframe>
}

@code {
    [Parameter]
    public string PdfPath { get; set; } = string.Empty;
    

    private string PdfUrl => $"/pdf?path={Uri.EscapeDataString($@"\\plbie01as15\Labelprint\Hartownia\InstructionFinder\{PdfPath}.pdf")}";
    ///private string PdfUrl => $"/pdf?path={Uri.EscapeDataString(PdfPath)}";
}